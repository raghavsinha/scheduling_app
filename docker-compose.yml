services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: scheduler
      POSTGRES_PASSWORD: scheduler
      POSTGRES_DB: scheduler
    ports:
      - '5432:5432'
    volumes:
      - scheduling_postgres:/var/lib/postgresql/data
volumes:
  scheduling_postgres:
